# wikiquote-scraper
A small NLP project that scrapes multiple Wikiquote pages to build a text dataset. It also includes a simple pipeline that scrapes a single page, tokenizes the text, and trains a toy language model for text generation.
